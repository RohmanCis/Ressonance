# Current Task Status

**Status:** WAITING_FOR_AGENT

Active task: T041 — guest-flow fixes 1–5 from previous session's priority list,
single commit. Plus DESIGN.md §5.6 Done-screen spec sync (librarian lane, then
orchestrator applies canonical edit).

- fixer lane: mic/voice-timer unmount leak, syncPhotos mutex, start() guard,
  handleCapture try/catch + use-camera teardown, object-URL revocation.
- librarian lane: draft §5.6 spec text matching current Done.tsx implementation.

See task.md for the full contract.
