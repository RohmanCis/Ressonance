/**
 * Placeholder for generated Supabase database types (from
 * `supabase gen types`). The schema is not yet generated; until then the
 * Database type is unknown and unused by tests/typecheck.
 * TODO: regenerate once migrations exist.
 */
export type Database = Record<string, never>;